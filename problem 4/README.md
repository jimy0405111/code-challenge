# Express TypeScript CRUD API

A simple CRUD backend using ExpressJS, TypeScript, and SQLite with TypeORM.

## Requirements

- Node.js >= 18
- npm

## Installation

```bash
git clone <repo-url>
cd express-ts-crud
npm install
